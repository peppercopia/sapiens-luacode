

local clientAlpaca = {}

--local clientGOM = nil

clientAlpaca.serverUpdate = function(object, pos, rotation, scale, incomingServerStateDelta)

end

clientAlpaca.objectWasLoaded = function(object, pos, rotation, scale)

end

function clientAlpaca:update(object, dt, speedMultiplier)
    
end

function clientAlpaca:init(clientGOM_)
    --clientGOM = clientGOM_
end

return clientAlpaca