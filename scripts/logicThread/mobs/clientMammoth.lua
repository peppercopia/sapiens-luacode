

local clientMammoth = {}

--local clientGOM = nil

clientMammoth.serverUpdate = function(object, pos, rotation, scale, incomingServerStateDelta)

    
end

clientMammoth.objectWasLoaded = function(object, pos, rotation, scale)

end

function clientMammoth:update(object, dt, speedMultiplier)
    
end

function clientMammoth:init(clientGOM_)
    --clientGOM = clientGOM_
end

return clientMammoth